// const fs = require('fs')
// for(const i of Array(100).keys()) {
//     managerSQL = `INSERT INTO public.manager (manager_id, manager_name) VALUES (${i}, 'manager_${i}');`
//     regionSQL = `INSERT INTO public.region (region_id, region_name, manager_id) VALUES (${i}, 'Pittsburgh${i}', ${i});`
//     storeSQL`INSERT INTO public.store (store_id, store_name, region_id) VALUES (1, 'Target', 1);`
//     INSERT INTO public.salesperson (sp_id, store_id, sp_name) VALUES (1, 1, 'Anj');
// }
// for(const i of Array(100).keys()) {
// }